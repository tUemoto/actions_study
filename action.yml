name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  kid:  # id of input
    description: 'kid'
    required: true
    default: ''
  iss:
    required: true
    default: ''
  private_key:
    required: true
    default: ''
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'index.js'
