name: Build
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "18.16.0"
    - name: package install
      run: npm i
      shell: bash
